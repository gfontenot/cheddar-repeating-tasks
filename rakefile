require './lib/tasks/cheddar_tasks'

desc 'Create task'
task :cheddar do
  tasks = ["Pay Bill A", "Pay Bill B", "Pay Bill C #bills"]
  tasks.each do |text|
    Cheddar::TaskCreator.new(text)
  end
end
